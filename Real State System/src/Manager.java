public class Manager extends Agent {
    // constructors
    public Manager() {
    }

    public Manager(String id, String username, String password, String firstName, String lastName, String phoneNumber, String emailAddress, double credit, String realEstates) {
        super(id, username, password, firstName, lastName, phoneNumber, emailAddress, credit, realEstates);
    }
}
